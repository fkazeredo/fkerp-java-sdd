# Release 0.34.0 — Fase 19b · Quarentena de integração + revisão dirigida do decision-log

- **Data:** 2026-07-02
- **Fase do ROADMAP:** 19 (fatia 19b de 12 — Refactoring de maturidade)
- **Specs entregues:** SPEC-0009 (BR7 revisada, **BR10** nova), SPEC-0016 (**BR8** nova), SPEC-0023 (nota de revisão), SPEC-0024 (—)
- **Decisões:** **DL-0120** (quarentena de inbound — revisa a DL-0017), **DL-0121** (Anexo/Fator R + flag de emissão — refina a DL-0044); adendos de revisão nas DLs 0009/0017/0029/0044/0049/0058/0070/0074
- **Tag git:** `0.34.0`

## Destaques

Segunda fatia da Fase 19: a **revisão séria do decision-log** pedida pelo dono, com pesquisa por
decisão e as mudanças aplicadas em código.

- **Quarentena de integração (DL-0120, revisa a DL-0017).** Um inbound do site de cotação
  rejeitado por conta desconhecida **não se perde mais**: o 422 externo permanece, mas o payload
  traduzido é preservado em `inbound_quarantine` (transação própria, idempotente por id externo,
  índice único parcial). A tela **Origem de ofertas** ganha a seção **Quarentena de integração**:
  listar, **reprocessar** (cria a cotação INTEGRATED quando a causa foi corrigida; causa
  persistente mantém pendente) e **descartar**. Falha de assinatura **não** quarentena nada.
- **Tributário refinado (DL-0121, refina a DL-0044).** A pesquisa de enquadramento achou a nuance
  que agrava o risco: agência (CNAE 7911-2/00) → Anexo III sem Fator R, mas **representação
  comercial (GSA) → Anexo V com Fator R**. Registrada na SPEC-0016 (Q7) para o contador + flag
  **`billing.tax.regime-confirmed`** (default `false`) que o validador de produção (19c) exigirá
  para emissão real de NFS-e.
- **Mantidas com justificativa reforçada** (adendo "Revisão Fase 19b" em cada arquivo):
  DL-0009 (fórmula de preço), DL-0029 (REP-P), DL-0049 (settlementRate/marco cambial),
  DL-0058 (tombstone LGPD), DL-0070 (CLT art. 59), DL-0074 (AES-GCM + recomendação **A1**/KMS).
- **Regra enum→cadastro codificada** (pedido do dono): CLAUDE.md invariante 7 +
  `architecture/backend.md` seção "Enums vs cadastro" (ADR-0019 vira regra permanente).

## Adicionado

- Módulo `sourcing`: `InboundQuarantineEntry/Status/Repository/View/Service` + exceções
  (404 `sourcing.quarantine.not-found`, 409 `sourcing.quarantine.not-pending`).
- Endpoints aditivos: `GET /api/sourcing/inbound-quarantine[?status=]`,
  `POST /api/sourcing/inbound-quarantine/{id}/replay|discard` (papel OPERATIONS pela matriz 19a).
- Tela Sourcing: seção "Quarentena de integração" (carregar/reprocessar/descartar, i18n pt/en).
- Migração **V37** (`inbound_quarantine` + índice único parcial de pendência).
- Config `billing.tax.regime-confirmed` (default false; enforcement na 19c).
- Testes: `InboundQuarantineIntegrationTest` (5 cenários E2E) + 3 testes de service no frontend.

## Alterado

- `QuotationSiteInboundController`: rejeição de negócio quarentena antes de propagar o 422.
- SPEC-0009 (BR7 anotada/BR10), SPEC-0016 (BR8), SPEC-0023 (nota A1/KMS); INDEX do decision-log
  com as revisões; CLAUDE.md/backend.md (regra enum→cadastro).
- OpenAPI (versão → 0.34.0).

## Migrações

- **V37__create_inbound_quarantine.sql**.

## Impacto em OpenAPI / contratos

- **O webhook não muda** (mesma 422). Endpoints novos são aditivos. Nenhum shape existente mudou.

## Portões

- Backend `./mvnw verify` verde (novos testes de quarentena; suíte 19a intacta).
- Frontend `ng lint`/`ng test` (cobertura acima dos pisos)/`ng build` verdes.

## Riscos

- A quarentena guarda o comando traduzido (documento da conta, produto, preço) — dados
  comerciais, não PII sensível; a leitura da lista é autenticada e as ações exigem OPERATIONS.
- O gate de emissão real (`regime-confirmed`) só passa a **bloquear** com o validador da 19c —
  até lá é configuração documentada (o gateway de NFS-e segue mock).
