# Caderno de testes — Fatia 8i-3 · People: holerite arquivado no Compliance (PAYROLL)

- **Spec:** SPEC-0022 (BR5/BR6) + SPEC-0008 (cofre)
- **Branch:** `feature/8i-3-payslip` → `feature/8i-integration`
- **Sem migração nova** (reusa o cofre da SPEC-0008 e o `employees` da V27)

## Escopo (Acceptance Criteria cobertos)

- BR5 — "Holerite/espelho processado MUST ser arquivável no Compliance (PAYROLL) com retenção 5 anos."
- BR6 — People não vira cofre; o legal vive no Compliance; `documentId` por valor.

## Casos de teste

### Integração (Testcontainers + Postgres) — `PayslipArchivingIntegrationTest` (2 casos)
| Caso | Verifica | Regra |
|---|---|---|
| `archivesAPayslipAsPayrollWithFiveYearRetentionAndPersonalData` | arquiva → Document `PAYROLL`, `retention_until = +5 anos`, `has_personal_data=true` | BR5/DL-0072 (LGPD) |
| `archivingForAnUnknownEmployeeIsRejected` | colaborador inexistente → `EmployeeNotFoundException` (404) | DL-0072 (valida existência) |

### Arquitetura/portões
- `ArchitectureTest` (14), `ModularityTests` (acíclico — orquestrador em `infra`, não fura fronteira),
  `HttpErrorMappingCompletenessTest` — ✅. O domínio People **não** depende do `ComplianceService`
  (o orquestrador `PayslipArchivingService` vive em `infra.integration.payroll`, BR6).

## Resultado

`./mvnw verify` → **BUILD SUCCESS**, **411 testes**, 0 falhas/erros. OpenAPI atualizada (descrição
People + versão 0.17.0). Log de negócio `PayslipArchived` sem PII; acesso ao documento auditado pelo
cofre (LGPD).

## Cobertura — o que NÃO está coberto (e por quê)
- Vínculo automático do `documentId` retornado de volta ao `Employee` (`contract_document_id`) não foi
  fiado nesta fatia — o holerite é PAYROLL avulso (o contrato de trabalho é referenciado por valor no
  cadastro). Reaberto só se o RH pedir o vínculo automático holerite→colaborador.
- Verificação de assinatura do holerite — holerite **não** é artefato assinado legal (isso é o AFD/AEJ,
  SPEC-0012); aqui é dado operacional de RH com retenção fiscal.

## Como reproduzir
```bash
cd backend
./mvnw -o test -Dtest='PayslipArchivingIntegrationTest'
./mvnw -o verify
```
