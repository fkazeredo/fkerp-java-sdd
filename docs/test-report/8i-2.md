# Caderno de testes — Fatia 8i-2 · People: jornada do período + banco de horas + divergências

- **Spec:** SPEC-0022 (BR2/BR3/BR4)
- **Branch:** `feature/8i-2-journey` → `feature/8i-integration`
- **Reusa migração:** `V27` (tabelas `journeys`, `journey_discrepancies`)

## Escopo (Acceptance Criteria cobertos)

- "Ao chegar o snapshot do período, montar a jornada e o saldo do banco de horas."
- "Marcação inconsistente vira divergência para tratamento humano" (alerta, não corrige — BR4).
- Regressão: o snapshot **nunca** vira documento legal aqui (BR6).
- `./mvnw verify` verde.

## Casos de teste

### Unitário/domínio — `JourneyCalculatorTest` (7 casos)
| Caso | Verifica | Regra |
|---|---|---|
| `computesPositiveBalanceWhenWorkedExceedsContracted` | 176:20 − 176:00 = +00:20 (exemplo da spec) | BR3/DL-0070 |
| `computesNegativeBalanceAsFaltas` | banco negativo `-06:00` | BR3/DL-0070 |
| `detectsOddPunch` | marcações ímpares → `ODD_PUNCH` (+`MISSING_PUNCH`) | BR4/DL-0071 |
| `detectsMissingPunchEvenWhenEven` | menos marcações que o esperado → `MISSING_PUNCH` | BR4/DL-0071 |
| `detectsIncoherentJournalWhenPunchesButNoWork` | marcações sem trabalho → `INCOHERENT_JOURNAL` | BR4/DL-0071 |
| `cleanEvenFullJourneyHasNoDiscrepancy` | jornada par e completa → sem divergência | BR4 |
| `rejectsNegativeCounts` | contagem negativa → `JourneyInvalidException` | Validation |

### Integração (Testcontainers + Postgres) — `JourneyApiIntegrationTest` (6 casos)
| Caso | Verifica | Regra |
|---|---|---|
| `processesAJourneyFromTheSnapshotAndYieldsTheTimeBank` | processa do snapshot → 201; timebank `176:20`/`176:00`/`+00:20` | BR2/BR3 |
| `reProcessingTheSamePeriodIsIdempotent` | reprocessar não duplica jornada (atualiza no lugar) | BR2/DL-0069 |
| `anOddPunchRaisesADiscrepancyAlertWithoutAutoCorrecting` | `ODD_PUNCH` na fila + contado no timebank; reprocessar não duplica alerta | BR4/DL-0071 |
| `processingWithNoSnapshotForThePeriodIsRejected` | sem snapshot do período → 400 `people.journey.invalid` | DL-0069 |
| `unknownJourneyIs404` | jornada não processada → 404 `people.journey.not-found` | Error Behavior |
| `regressionProcessingCreatesNoLegalDocument` | processar **não** cria `Document` no Compliance (`documents` vazio) | **BR6 (regressão)** |

### Arquitetura/portões
- `ArchitectureTest` (14), `ModularityTests` (acíclico), `HttpErrorMappingCompletenessTest` (as
  `Journey*` exceções mapeadas) — ✅.

## Resultado

`./mvnw verify` → **BUILD SUCCESS**, **409 testes**, 0 falhas/erros. Eventos `JourneyProcessed`
e `JourneyDiscrepancy` publicados in-process; logs de negócio sem PII (equivalente às métricas
`journeys_processed_total`/`journey_discrepancies_total`; o wiring Micrometer/Prometheus é a Fase 11).

## Cobertura — o que NÃO está coberto (e por quê)
- Resolução de divergência (RESOLVED) tem suporte no modelo/serviço, mas o endpoint de resolução não
  foi exposto no v1 (a fila de tratamento + leitura cobrem o critério de aceite; resolver é ato
  humano fora do escopo mínimo). Reaberto só se o RH pedir.
- `workingDays` por período é entrada operacional (não há calendário de feriados — Regra Zero,
  DL-0069); calendário de dias úteis = folha (comprar/integrar).
- Arquivo de holerite no Compliance → fatia **8i-3**.

## Como reproduzir
```bash
cd backend
./mvnw -o test -Dtest='JourneyCalculatorTest,JourneyApiIntegrationTest'
./mvnw -o verify
```
